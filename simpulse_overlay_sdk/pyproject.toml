[project]
name = "simpulse-overlay-sdk"
version = "0.1.0"
description = "SimPulse In-Game HUD Overlay Widget SDK — Contracts for third-party HUD widgets"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "simpulse-sdk>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
