[project]
name = "simpulse-race-engineer-sdk"
version = "0.1.0"
description = "SimPulse Virtual Race Engineer & Spotter Role Contracts & Models SDK"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "simpulse-sdk>=0.1.0",
    "isimotor-rawudp-client>=0.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
